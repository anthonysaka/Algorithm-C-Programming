#include <stdio.h>
#include <string.h>

int main()
{
/*
char numero1[20],numero2[20];
unsigned int num1=0,num2=0;

printf("Escriba un numero: " );
scanf( "%d",&num1);

printf("Escriba otro numero: " );
scanf( "%u",&num2);

sprintf( numero1,"%d",num1);
sprintf( numero2,"%d",num2);

int cant_n1=strlen(numero1);
int cant_n2=strlen(numero2);

if (cant_n1>cant_n2) {
  printf("\nEl numero %d tiene mas digitos que el numero. %d",num1,num2 );
}
else if (cant_n1=cant_n2) {
printf("\nEl numero %d y el numero %d tienen igual cantidad de digitos.",num1,num2 );
}
  else{
  printf("\nEl numero %d tiene mas digitos que el numero. %d",num2,num1 );
  }

printf("\n\nEl numero %d tiene %d digitos",num1,cant_n1);
printf("\nEl numero %d tiene %d digitos",num2,cant_n2);
*/

return 0;
}
